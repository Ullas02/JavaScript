// strings in JS

let name2 = "Joesph"
console.log("Name : " + name2);

// template literals (String intepolation : we can insert variables directly in template literal) , it has backtik.

let frnd = `My name is ${name2}`;
console.log(frnd);

// escape sequence characters (\n \t \r)

let fruit = 'bana\'na';
console.log(fruit)